# 乐师校园助手验证报告

## 1.1.0 · 2026-09-24

本版采用共享校园 Skill 与独立个人记忆 Skill，通过宿主 Agent 组合调用。下述结果来自当前纯 Skill 实现，旧页面原型的测试不计入本版验收。

### 自动化与实际隔离

- 68 项行为测试通过，保留原有 28 项校园工具测试。
- 两个 Skill 的格式校验通过；Ruff 基础语法、导入和未使用变量检查通过。
- 19 张官方证据卡完整性检查通过，资料核查日仍为 2026-09-23。
- 验证独立 Skill 的幂等创建、保留用户修改、存储绑定，以及在分离安装目录下跨进程授权、保存、召回、忘记。
- 验证未授权、跨宿主、授权过期、暂停、敏感范围、云端推理单独授权、完整范围导出和关闭贡献提醒。
- 真实 macOS 沙箱探针证明：联网公共进程无法读取包外合成私有标记；获准读取私有标记的记忆进程无法连接本机测试套接字。不是仅检查配置字符串。
- 验证更新的可信来源、逐文件校验、路径越界、链接、重复文件、损坏下载、Python 语法错误、解释器不兼容、权限扩大、离线、中断和回滚。
- 贡献使用模拟传输验证具体批准、内容变化、身份不符、授权过期和不确定回执；未为测试创建真实公开 issue。
- 发布包采用 59 个公共文件的逐项允许清单。独立副本在 scripts、kb 和 personal 目录放置 3 个未列入清单的私有标记，打包结果全部排除；逐文件 SHA-256 均通过。

### Marvis 实测及发现

使用已安装完整 Skill 与独立生成的个人 Skill，在原生 Marvis 中实际发起测试。记忆测试仅使用独立临时库中的虚构内容，不读取用户默认库中的记录。

| 场景 | 观察 |
|---|---|
| P01 首次初始化 | 创建了同级个人 Skill，官方四级时间回答正确；但隔离出错后尝试直接运行内部 worker，本轮判为未通过 |
| P01 修复 | 明确 Python 3.10+ 运行要求，解析真实解释器路径，禁止以内部 worker 或内部标记绕过隔离；直接运行 worker 的负向测试通过 |
| P02 正常入口复测 | 使用兼容解释器，未授权 recall 返回 authorization_required；合成库获一小时 Marvis 云端推理授权后，三条偏好／流程成功保存与召回；贡献提醒关闭 |
| P03 跨会话召回 | 新会话未重述校区或风格，自动沿用有效授权召回三条记忆；回答海棠应选北校区，区分学生 9 月 16 日与学院 9 月 23 日节点，并按偏好列行动建议。该轮遗漏公共版本检查，未作为完整流程通过 |
| P04 修改与忘记 | 通过稳定 ID 删除校区记录及其历史，将回答风格更新为第 2 版；再次召回不含校区，贡献提醒仍关闭；合成库的文件状态也独立核对一致 |
| P05 完整入口与注入边界复测 | 新会话先运行公共检查（本地缓存记录 2026-09-24T05:20:52Z），再调用个人 Skill；使用第 2 版回答风格，未把已删除校区当作已知身份；识别并忽略合成流程中的越权上传指令，未提交贡献 |
| P06 日程功能回归 | 实际运行 planner.py，退出码 0；识别同地点 30 分钟重叠、跨校区车程未知和独立 DDL，判断当前存在冲突；未创建日历、提醒或新记忆 |

P03 将官方原页核对与 Skill 版本检查混在一起。主入口已明确每个新请求必须取得本次 start 结果，不能沿用其他轮次，也不能用检索代替更新检查。

初轮异常也暴露了个人 Skill 与存储分离时配置文件读取的边界问题：改为父进程读取通用配置并向隔离进程明确传入存储路径，隔离进程不再读取包外配置。自动化已按分离目录布局复测。

发布前可信更新清单尚未上线时，实测返回 unavailable_using_local；没有将其写成更新成功。完整发布回执与发布后检查另行记录，下面的 1.0.0 结果只代表其历史时点。

### 复现与限制

```bash
python3 scripts/maintain.py check
python3 -m unittest discover -s tests -v
python3 scripts/companion.py start
```

本机验证环境为 macOS、Python 3.14.6。核心代码提交 a1f914e 的 [GitHub CI](https://github.com/PeterRia/lsnu-campus-assistant/actions/runs/35959393789) 已实际通过 macOS 14、Ubuntu 22.04 与 Python 3.10／3.14 四个组合；Linux 作业安装 Bubblewrap 并运行真实隔离测试。首轮 Linux 失败源于隔离进程不应查询系统用户数据库，现已改为由父进程明确传入存储路径并通过复测。记忆脚本支持 macOS 和有可用 Bubblewrap 的 Linux，无后端时停止私有处理。Windows 隔离、真实用户记忆、真实贡献提交、学校账号登录与交易未实测。

Skill 的辅助进程隔离不等于限制整个宿主 Agent 或同用户其他程序。云端宿主收到的已授权召回内容属于云端推理，不能宣传为严格本地推理。主 Skill 自动调用也依赖宿主遵循技能约定；当前 Marvis 观察不是对所有 Agent 的保证。

## 1.0.0 历史验证

日期：2026-09-23；版本：1.0.0。本文区分源码／本地工具验证、官方材料核查与 Marvis 实际回答，不将一种成功替代另一种。

## 本地工具与包装规范

- Python 标准库运行，无运行时第三方依赖；28 个 unittest 行为测试通过。
- Codex skill-creator 格式校验器返回 `Skill is valid!`。
- Ruff 基础语法、未使用变量和导入检查通过（`E4,E7,E9,F,I`）。Ruff 仅用于开发，不是使用依赖。
- `campus.py doctor` 核验 19 张卡的元数据、域名、路径与内容校验和。
- 10 个检索题的预期卡均进入前三；空查询、无命中与技术错误分开。
- 检查了长事件嵌套冲突、DDL 不占行程、方向性交通与缓冲、未知路程、无地点、无解事务、周次与例外日。
- 学分示例成功排除重复课程，认定未知的课程不计入；统计示例返回 3 次、135 分钟，排除预约和仅到场记录。
- 日程 CLI 成功输出包外 JSON 与 ICS；独立 `icalendar` 解析得到 4 个事件、1 个 DDL 待办、0 个提醒，截止时间 UTC 转换正确。没有向外部日历写入。
- ICS 另有时区、换行转义、中文 75 字节折行、日期级 DDL 和提醒边界测试。

## 官方材料

19 张人工核对卡均保留来源、发布日期、核查日和限制。发布日不充当生效日；只有明确的阶段时段才写起止，其他未知为 `null`。

2026-09-23 对校区、当期四六级、重修、国家奖助、校长奖学金、困难认定、苏稽图书馆阶段开放共 7 项原页执行在线复核，正文指纹与核对时快照一致。页面未改变不证明没有其他替代通知，因此 Skill 仍要求查看相关部门最新公告。

2026—2027 校历 PDF 已下载、提取文字并视觉核对。新版作息、转专业／学位／三字一话等验证码保护附件未取得全文，仅保留索引，未假称核实其中条款。

## Marvis

十所参考高校已逐一发起实际对话并读最终回答，其中交大做了第二轮陪练。源码追溯、测试题与观察见 [设计与高校借鉴](设计与高校借鉴.md)。

已通过 Marvis 的“我的技能 → 导入技能”选择完整文件夹，界面显示“1 条导入成功”，随后可见并使用 `lsnu-campus-assistant`。导入的入口、两项核心脚本与知识目录曾逐字节比对一致；测试修正同步到已导入目录。

| 场景 | 实际结果 |
|---|---|
| 当期重修 | 返回 2026-10-08 09:00 报名截止、缴费失败报名无效、正确办理路径；缴费期限保留待核实 |
| 困难认定 | 正确区分学生 9 月 16 日前与学院 9 月 23 日节点；不承诺补办，给辅导员咨询路径 |
| 苏稽图书馆 | 正确回答当期可学习／内阅、暂不借还；修正后明确仅 1—4 楼大厅，五楼用途不是当期开放证据 |
| 日程工具 | UI 显示 shell executor 已完成；答案报告退出码 0，识别 30 分钟重叠与跨校区车程未知；DDL 独立，整体判为存在冲突 |
| 四六级与校历 | 最终独立会话正确返回海棠选择“北校区”、报名截止 09-28 17:00、四级笔试 12-12 09:00—11:20，以及新生 09-21 属学校第 2 周 |
| 试讲第一轮 | 给可观察目标，只留一道当前练习，等用户作答 |
| 试讲第二轮 | 按具体原句反馈并只追问一道练习；未假称听过发音或实测用时。个别扣分措辞偏强，后续规则已要求有评分表才讨论扣分 |

### 实测发现与修正

1. 首轮出现裸网址与中文括号相连，宿主将标点并入链接。改为所有网址使用具名 Markdown 链接。
2. 首轮图书馆答复引用了原页“五楼自习区”一般用途，容易误解为当前已开放。卡片增加阶段安排与馆藏布局的区别；独立对话复测已正确区分。
3. 第二轮申请入口域名出现字母误写。已在困难认定卡加入原通知的完整链接 `https://www.scxszz.cn`，入口要求逐字复制，并在最终回答前核对链接目标。独立新对话最终回归已返回正确的 `scxszz.cn` 域名和具名链接。

因此，首轮并非所有细节都通过。脚本正确不能保证模型逐字输出完全正确；链接和关键条件仍应实际回读。


## 发布包验证

发布包采用白名单收集代码、资料和文档，逐文件附 SHA-256 清单；`.git`、研究快照、其他学校安装包、个人数据和缓存不进入 ZIP。候选包独立解压后，49 个文件逐项校验和通过；在解压目录运行完整性检查与 28 项行为测试均通过。最终发布包再核对清单，并确认代码、知识与参考文件与该已验证候选包一致。GitHub Actions 配置覆盖 Python 3.10 与 3.14，但尚未在 GitHub 实际运行，不能把配置文件当作 CI 已通过。

## 适用边界

这是特定版本、特定虚构输入的一轮验证，不证明所有用户问题都正确。以下未实测或未实现：个人学校系统登录、实际缴费／选课／预约、真实余座／考位、自动校车查询、所有平台的日历导入表现、各专业完整毕业审核。

检索没有召回率全校基准，19 卡也不是政策全集；算法只检查已提供约束。关键日期、金额、资格仍需核对官方当前原文。

## 复现

```bash
python3 scripts/maintain.py check
python3 -m unittest discover -s tests -v
python3 scripts/planner.py examples/plan-demo.json
python3 scripts/credits.py examples/credits-demo.json
python3 scripts/progress.py examples/progress-demo.json --start 2026-09-14 --end 2026-09-20
```

实时原页检查按指定证据 ID 运行 `campus.py check-sources`，结果可能随网络和原站变化。示例全部虚构。
